# Документация системы распознавания лиц

## Содержание
1. [Общее описание системы](#общее-описание-системы)
2. [Архитектура приложения](#архитектура-приложения)
3. [Структура проекта](#структура-проекта)
4. [Описание основных компонентов](#описание-основных-компонентов)
5. [Процесс работы системы](#процесс-работы-системы)
6. [Инструкция по использованию](#инструкция-по-использованию)
7. [Технические детали реализации](#технические-детали-реализации)

---

## Общее описание системы

Система распознавания лиц представляет собой программное приложение, разработанное на языке Python, предназначенное для автоматического обнаружения и идентификации конкретных людей (Aleksander и Egor) в видеопотоке с веб-камеры или на статических изображениях.

### Основные возможности системы:

- **Распознавание лиц в реальном времени** — обработка видеопотока с веб-камеры с отображением результатов на экране
- **Обработка изображений** — распознавание лиц на отдельных фотографиях или в папках с изображениями
- **Автоматическая обработка загрузок** — мониторинг папки `uploads` и автоматическая обработка новых изображений
- **Захват обучающих данных** — встроенная функция для захвата фотографий с веб-камеры для пополнения датасета
- **Обучение модели** — автоматическое извлечение эмбеддингов и вычисление эталонных представлений (центроидов)

### Технологический стек:

- **Python 3.10+** — основной язык программирования
- **face_recognition** — библиотека для обнаружения лиц и извлечения эмбеддингов
- **OpenCV (cv2)** — работа с видеопотоком и обработка изображений
- **NumPy** — численные вычисления и работа с массивами
- **scikit-learn** — машинное обучение (SVM классификатор, PCA)
- **CustomTkinter** — современный графический интерфейс пользователя
- **dlib** — низкоуровневая библиотека для компьютерного зрения

---

## Архитектура приложения

Система построена по модульному принципу, что обеспечивает разделение ответственности между компонентами и упрощает поддержку кода.

### Основные модули:

```
FaceRecognitionProject/
├── main.py                 # Точка входа в приложение
├── config.py               # Конфигурация системы
├── src/
│   ├── face_recognizer.py  # Ядро системы распознавания
│   ├── train_model.py      # Обучение модели
│   ├── gui_app.py          # Графический интерфейс
│   ├── file_processor.py   # Обработка файлов
│   └── dataset_utils.py    # Управление датасетом
├── dataset/                # Обучающие данные
│   ├── Aleksander/         # Фотографии Aleksanderа
│   └── Egor/               # Фотографии Egorа
├── models/                 # Сохраненные модели
│   ├── embeddings.pkl      # Эмбеддинги лиц
│   ├── centroids.pkl       # Центроиды классов
│   └── classifier.pkl      # SVM классификатор (опционально)
├── uploads/                # Папка для автоматической обработки
└── results/                # Результаты обработки
    ├── images/             # Обработанные изображения
    └── reports/            # Отчеты о работе
```

### Поток данных:

```
Входные данные (камера/изображение)
    ↓
Детекция лиц (OpenCV Haar Cascade)
    ↓
Извлечение эмбеддингов (face_recognition)
    ↓
Сравнение с эталонами (евклидово расстояние)
    ↓
Классификация (центроиды или SVM)
    ↓
Визуализация результатов
```

---

## Структура проекта

### 1. main.py

Главный файл приложения, выполняющий следующие функции:
- Проверка наличия необходимых директорий
- Вывод статистики датасета при запуске
- Проверка наличия обученных моделей
- Запуск графического интерфейса

**Основные функции:**
- `main()` — инициализация системы и запуск GUI

### 2. config.py

Файл конфигурации, содержащий все настройки системы:

**Пути к директориям:**
- `DATASET_DIR` — папка с обучающими данными
- `MODELS_DIR` — папка для сохранения моделей
- `UPLOADS_DIR` — папка для автоматической обработки
- `RESULTS_DIR` — папка для результатов

**Параметры распознавания:**
- `DISTANCE_THRESHOLD` — пороговое значение расстояния (по умолчанию 0.6)
- `SCALE_FACTOR` — коэффициент уменьшения разрешения для ускорения (0.25)
- `CAMERA_INDEX` — индекс камеры (0 — первая доступная)

**Параметры производительности:**
- `PROCESS_EVERY_N_FRAMES` — обрабатывать каждый N-й кадр (3)
- `CAMERA_WIDTH/HEIGHT` — разрешение камеры (580x580)
- `CACHE_RESULTS_FRAMES` — кэширование результатов на N кадров (5)

**Метки классов:**
- `LABELS` — словарь соответствия индексов классов именам людей

### 3. src/face_recognizer.py

Основной класс системы распознавания `FaceRecognizer`.

**Основные методы:**

- `__init__(use_svm=False)` — инициализация распознавателя
  - Загрузка детектора лиц OpenCV (Haar Cascade)
  - Загрузка обученных моделей (центроиды, SVM)

- `load_models()` — загрузка сохраненных моделей из файлов
  - Загрузка центроидов из `centroids.pkl`
  - Загрузка SVM классификатора (если используется)

- `detect_faces_opencv(frame, scale_factor)` — детекция лиц с использованием OpenCV
  - Преобразование в grayscale
  - Применение Haar Cascade детектора
  - Возврат координат ограничивающих прямоугольников

- `recognize_faces(frame, use_scale=True)` — основной метод распознавания
  - Детекция лиц на кадре
  - Извлечение эмбеддингов для каждого обнаруженного лица
  - Сравнение с эталонными представлениями
  - Возврат результатов распознавания

- `draw_results(frame, results)` — визуализация результатов
  - Отрисовка прямоугольников вокруг лиц
  - Вывод имени и уверенности распознавания
  - Цветовая индикация (зеленый — Aleksander, синий — Egor, красный — Unknown)

- `get_model_info()` — получение информации о загруженных моделях

**Алгоритм распознавания:**

1. **Детекция лиц:** Используется Haar Cascade детектор OpenCV для быстрого обнаружения лиц на кадре
2. **Извлечение признаков:** Для каждого обнаруженного лица вычисляется 128-мерный эмбеддинг с помощью предобученной нейронной сети из библиотеки `face_recognition`
3. **Сравнение с эталонами:** Вычисляется евклидово расстояние между эмбеддингом обнаруженного лица и центроидами известных классов
4. **Принятие решения:** Если минимальное расстояние меньше порогового значения, лицо классифицируется как известное, иначе — как Unknown

### 4. src/train_model.py

Класс `FaceTrainer` для обучения модели распознавания.

**Основные методы:**

- `extract_embeddings()` — извлечение эмбеддингов из датасета
  - Обход всех папок в `dataset/`
  - Загрузка изображений и извлечение эмбеддингов
  - Сохранение эмбеддингов и меток в `embeddings.pkl`
  - Возврат массивов эмбеддингов и меток

- `compute_centroids()` — вычисление центроидов классов
  - Загрузка эмбеддингов из файла
  - Группировка по классам
  - Вычисление среднего значения (центроида) для каждого класса
  - Сохранение центроидов в `centroids.pkl`

- `train_classifier()` — обучение SVM классификатора (опционально)
  - Разделение данных на обучающую и тестовую выборки
  - Обучение линейного SVM классификатора
  - Оценка точности на тестовой выборке
  - Сохранение модели в `classifier.pkl`

- `train_full_model()` — полный цикл обучения
  - Последовательное выполнение всех этапов обучения
  - Проверка успешности каждого этапа

**Процесс обучения:**

1. **Извлечение эмбеддингов:** Для каждого изображения в датасете вычисляется эмбеддинг лица
2. **Вычисление центроидов:** Для каждого класса (Aleksander, Egor) вычисляется усредненный вектор признаков
3. **Обучение SVM (опционально):** Создается и обучается классификатор для альтернативного метода распознавания

### 5. src/gui_app.py

Графический интерфейс пользователя на основе CustomTkinter.

**Класс `FaceRecognitionApp`:**

**Вкладки интерфейса:**

1. **Камера:**
   - Кнопка запуска/остановки видеопотока
   - Окно отображения видео с результатами распознавания
   - Счетчики распознаваний (Aleksander, Egor, Unknown)
   - Индикатор FPS

2. **Изображения:**
   - Выбор отдельного изображения или папки
   - Предпросмотр оригинального и обработанного изображения
   - Лог обработки

3. **Загрузки:**
   - Мониторинг папки `uploads` на новые файлы
   - Автоматическая обработка новых изображений
   - Лог обработанных файлов
   - Ручная проверка и очистка папки

4. **Настройки:**
   - Статистика датасета
   - Кнопки захвата фото для обучения
   - Кнопка обучения модели
   - Настройка порога распознавания (слайдер)
   - Информация о загруженной модели

**Основные методы:**

- `setup_ui()` — создание интерфейса
- `toggle_camera()` — запуск/остановка камеры
- `process_video()` — обработка видеопотока в отдельном потоке
- `capture_photos(person_name)` — захват фотографий для датасета
- `train_model()` — обучение модели в отдельном потоке
- `process_single_image()` — обработка одного изображения
- `process_folder()` — пакетная обработка папки
- `monitor_uploads()` — мониторинг папки загрузок

**Оптимизация производительности:**

- Обработка каждого N-го кадра (пропуск кадров)
- Уменьшение разрешения для обработки
- Кэширование результатов распознавания
- Ограничение частоты обновления GUI

### 6. src/file_processor.py

Класс `FileProcessor` для обработки файлов изображений.

**Основные методы:**

- `process_single_image(image_path, save_result)` — обработка одного изображения
  - Загрузка изображения
  - Распознавание лиц
  - Сохранение результата (опционально)
  - Возврат обработанного изображения и результатов

- `process_directory(directory_path)` — пакетная обработка папки
  - Поиск всех изображений в директории
  - Последовательная обработка каждого изображения
  - Сбор статистики обработки
  - Возврат словаря со статистикой

- `create_report(statistics, output_file)` — создание отчета
  - Формирование текстового отчета
  - Сохранение в файл
  - Возврат текста отчета

- `monitor_uploads_folder()` — мониторинг папки загрузок
  - Поиск новых изображений
  - Автоматическая обработка
  - Перемещение в архив

### 7. src/dataset_utils.py

Класс `DatasetManager` для управления датасетом.

**Основные методы:**

- `capture_photos(person_name, num_photos)` — захват фотографий с камеры
  - Открытие видеопотока
  - Автоматический или ручной захват кадров
  - Сохранение в соответствующую папку датасета
  - Возврат количества сохраненных фото

- `get_dataset_stats()` — получение статистики датасета
  - Подсчет фотографий для каждого человека
  - Возврат словаря со статистикой

- `add_lfw_dataset(lfw_path, max_per_person)` — добавление лиц из LFW датасета
  - Поиск изображений в указанной директории
  - Копирование в папку Unknown
  - Ограничение количества фото от одного человека

- `clear_dataset(person_name)` — очистка датасета
  - Удаление фотографий указанного человека или всего датасета

---

## Процесс работы системы

### Этап 1: Подготовка данных

1. **Захват фотографий:**
   - Пользователь запускает захват фото через интерфейс
   - Система открывает веб-камеру и сохраняет кадры в папку `dataset/[Имя_человека]/`
   - Рекомендуется захватить 50-60 фотографий для каждого человека под разными углами и освещением

2. **Структура датасета:**
   ```
   dataset/
   ├── Aleksander/
   │   ├── auto_001.jpg
   │   ├── auto_002.jpg
   │   └── ...
   └── Egor/
       ├── auto_001.jpg
       └── ...
   ```

### Этап 2: Обучение модели

1. **Извлечение эмбеддингов:**
   - Система загружает все изображения из папок датасета
   - Для каждого изображения обнаруживается лицо и вычисляется 128-мерный эмбеддинг
   - Эмбеддинги сохраняются в `models/embeddings.pkl` вместе с метками классов

2. **Вычисление центроидов:**
   - Для каждого класса вычисляется усредненный вектор признаков (центроид)
   - Центроиды сохраняются в `models/centroids.pkl`
   - Центроид представляет эталонное представление лица конкретного человека

3. **Обучение SVM (опционально):**
   - Создается и обучается линейный SVM классификатор
   - Модель сохраняется в `models/classifier.pkl`
   - SVM может использоваться как альтернатива методу центроидов

### Этап 3: Распознавание в реальном времени

1. **Захват кадра:**
   - Система получает кадр с веб-камеры через OpenCV

2. **Детекция лиц:**
   - Применяется Haar Cascade детектор для быстрого обнаружения лиц
   - Для ускорения обработки кадр может быть уменьшен в размере

3. **Извлечение признаков:**
   - Для каждого обнаруженного лица вычисляется эмбеддинг
   - Используется та же нейронная сеть, что и при обучении

4. **Сравнение с эталонами:**
   - Вычисляется евклидово расстояние между эмбеддингом обнаруженного лица и каждым центроидом
   - Находится минимальное расстояние и соответствующий класс

5. **Принятие решения:**
   - Если минимальное расстояние ≤ порогового значения → лицо распознано
   - Если минимальное расстояние > порогового значения → лицо помечается как Unknown

6. **Визуализация:**
   - Рисуется прямоугольник вокруг лица
   - Выводится имя и уверенность распознавания
   - Обновляются счетчики распознаваний

### Этап 4: Обработка изображений

1. **Одиночное изображение:**
   - Пользователь выбирает файл через интерфейс
   - Система обрабатывает изображение аналогично видеопотоку
   - Результат сохраняется в `results/images/`

2. **Пакетная обработка:**
   - Пользователь выбирает папку с изображениями
   - Система последовательно обрабатывает все изображения
   - Формируется отчет со статистикой обработки

3. **Автоматическая обработка:**
   - Система мониторит папку `uploads/`
   - При появлении нового изображения оно автоматически обрабатывается
   - Обработанный файл перемещается в `uploads/processed/`

---

## Инструкция по использованию

### Установка зависимостей

1. **Установка Python:**
   - Скачайте Python 3.10 или выше с официального сайта
   - Установите Python, убедившись, что опция "Add Python to PATH" включена

2. **Установка CMake:**
   - Скачайте CMake с официального сайта
   - Установите CMake (необходим для компиляции dlib)

3. **Установка библиотек:**
   ```bash
   pip install -r requirements.txt
   ```

   **Важно:** Если возникают проблемы с установкой `dlib`, используйте предкомпилированный wheel файл:
   ```bash
   pip install dlib-19.24.1-cp311-cp311-win_amd64.whl
   ```

### Первый запуск

1. **Запуск приложения:**
   ```bash
   python main.py
   ```

2. **Захват обучающих данных:**
   - Перейдите на вкладку "Настройки"
   - Нажмите "📸 Захватить фото Aleksanderа"
   - Система откроет окно с камерой
   - Нажмите 'c' для ручного захвата или дождитесь автоматического захвата
   - Повторите для Egorа
   - Рекомендуется захватить минимум 30-40 фото для каждого человека

3. **Обучение модели:**
   - После захвата фото нажмите "🎓 Обновить модель"
   - Дождитесь завершения обучения (в логе появится сообщение об успехе)
   - Модель будет сохранена в папке `models/`

### Работа с системой

#### Распознавание в реальном времени:

1. Перейдите на вкладку "Камера"
2. Нажмите "🚀 Запуск камеры"
3. Система начнет обработку видеопотока
4. На экране будут отображаться прямоугольники вокруг обнаруженных лиц с именами
5. Счетчики показывают количество распознаваний каждого человека
6. Для остановки нажмите "⏹️ Остановить камеру"

#### Обработка изображений:

1. Перейдите на вкладку "Изображения"
2. Нажмите "📁 Выбрать изображение" для одного файла или "📂 Выбрать папку" для пакетной обработки
3. Нажмите "🔍 Распознать"
4. Результаты отобразятся в правой панели
5. Обработанные изображения сохраняются в `results/images/`

#### Автоматическая обработка загрузок:

1. Перейдите на вкладку "Загрузки"
2. Убедитесь, что "Автоматическая обработка" включена
3. Поместите изображения в папку `uploads/`
4. Система автоматически обнаружит и обработает новые файлы
5. Обработанные файлы перемещаются в `uploads/processed/`

#### Настройка параметров:

1. Перейдите на вкладку "Настройки"
2. Используйте слайдер "Порог" для настройки чувствительности распознавания:
   - Меньшее значение (0.3-0.5) — более строгое распознавание, меньше ложных срабатываний
   - Большее значение (0.6-0.8) — более мягкое распознавание, больше ложных срабатываний
3. Рекомендуемое значение: 0.6

### Экспорт отчетов

1. Перейдите на вкладку "Настройки"
2. Нажмите "📤 Экспорт отчета"
3. Выберите место сохранения файла
4. Отчет будет содержать:
   - Статистику датасета
   - Настройки системы
   - Информацию о модели

---

## Технические детали реализации

### Алгоритм распознавания на основе центроидов

**Математическая формулировка:**

Для каждого обнаруженного лица вычисляется эмбеддинг \(E\) размерности 128. Затем вычисляется евклидово расстояние до каждого центроида \(C_k\):

\[d_k = \|E - C_k\| = \sqrt{\sum_{i=1}^{128} (e_i - c_{k,i})^2}\]

Где:
- \(E = (e_1, e_2, \ldots, e_{128})\) — эмбеддинг обнаруженного лица
- \(C_k = (c_{k,1}, c_{k,2}, \ldots, c_{k,128})\) — центроид класса \(k\)

Центроид вычисляется как среднее арифметическое всех эмбеддингов класса:

\[C_k = \frac{1}{N_k}\sum_{i=1}^{N_k}E_i\]

Где \(N_k\) — количество эмбеддингов в классе \(k\).

**Принятие решения:**

Лицо классифицируется как принадлежащее классу \(k^*\), если:

\[d_{k^*} = \min_k d_k \leq D_{threshold}\]

Где \(D_{threshold}\) — пороговое значение расстояния (по умолчанию 0.6).

### Оптимизация производительности

**Стратегии ускорения обработки:**

1. **Уменьшение разрешения:**
   - Кадры обрабатываются с коэффициентом масштабирования 0.25
   - Это уменьшает время детекции и извлечения признаков в ~16 раз

2. **Пропуск кадров:**
   - Обрабатывается каждый 3-й кадр (настраивается через `PROCESS_EVERY_N_FRAMES`)
   - Для промежуточных кадров используются кэшированные результаты

3. **Кэширование результатов:**
   - Результаты распознавания кэшируются на 5 кадров
   - Это позволяет избежать повторных вычислений для статичных сцен

4. **Оптимизация детектора:**
   - Использование Haar Cascade вместо более точных, но медленных методов
   - Упрощенные параметры детекции для скорости

5. **Ограничение частоты обновления GUI:**
   - GUI обновляется с частотой ~30 FPS (настраивается через `GUI_UPDATE_INTERVAL`)
   - Это снижает нагрузку на систему отрисовки

### Формат данных

**Эмбеддинги:**
- Тип: `numpy.ndarray`
- Форма: `(N, 128)`, где N — количество изображений
- Тип данных: `float64`
- Хранение: `pickle` формат в `models/embeddings.pkl`

**Центроиды:**
- Тип: `dict[int, numpy.ndarray]`
- Ключи: индексы классов (0 — Aleksander, 1 — Egor)
- Значения: массивы формы `(128,)`
- Хранение: `pickle` формат в `models/centroids.pkl`

**Метки классов:**
- Тип: `numpy.ndarray`
- Форма: `(N,)`
- Значения: 0 — Aleksander, 1 — Egor, -1 — Unknown
- Хранение: вместе с эмбеддингами в `embeddings.pkl`

### Обработка ошибок

Система включает обработку следующих типов ошибок:

1. **Ошибки загрузки моделей:**
   - Проверка существования файлов моделей
   - Вывод предупреждений при отсутствии моделей
   - Продолжение работы с ограниченной функциональностью

2. **Ошибки работы с камерой:**
   - Проверка доступности камеры
   - Обработка ошибок чтения кадров
   - Корректное освобождение ресурсов при закрытии

3. **Ошибки обработки изображений:**
   - Проверка корректности формата файлов
   - Обработка поврежденных изображений
   - Продолжение пакетной обработки при ошибках отдельных файлов

4. **Ошибки распознавания:**
   - Обработка случаев отсутствия лиц на изображении
   - Обработка ошибок извлечения эмбеддингов
   - Продолжение работы при частичных сбоях

### Расширяемость системы

Система спроектирована с учетом возможности расширения:

1. **Добавление новых людей:**
   - Создать новую папку в `dataset/` с именем человека
   - Добавить метку в `config.py` в словарь `LABELS`
   - Захватить фото и обучить модель

2. **Изменение метода распознавания:**
   - Можно переключиться на SVM классификатор через параметр `use_svm=True`
   - Можно реализовать другие методы сравнения эмбеддингов

3. **Добавление новых источников данных:**
   - Можно добавить поддержку IP-камер через изменение `VideoCapture`
   - Можно добавить обработку видеофайлов

4. **Улучшение интерфейса:**
   - Модульная структура GUI позволяет легко добавлять новые вкладки
   - Можно добавить графики статистики, историю распознаваний и т.д.

---

## Заключение

Данная документация описывает архитектуру, компоненты и принципы работы системы распознавания лиц. Система обеспечивает эффективное распознавание конкретных людей в реальном времени с использованием современных методов компьютерного зрения и машинного обучения.

Основные преимущества системы:
- Простота использования благодаря графическому интерфейсу
- Высокая производительность за счет оптимизаций
- Модульная архитектура для легкого расширения
- Поддержка различных режимов работы (камера, изображения, автоматическая обработка)

Система готова к использованию и может быть адаптирована под конкретные требования пользователя.
